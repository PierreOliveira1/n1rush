import React from 'react';

// Styles
import { Container } from '../../styles';

const Home = (): JSX.Element => {
	return (
		<Container>
			<h1>Home!!!</h1>
		</Container>
	);
};

export default Home;
